<%= form_with model: @podcast, local: true do |form| %>

  <% if @podcast.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@podcast.errors.count, "error") %> prohibited
        this podcast from being saved:
      </h2>
      <ul>
        <% @podcast.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= form.label :title %><br>
    <%= form.text_field :title %>
  </p>

  <p>
    <%= form.label :text %><br>
    <%= form.text_area :text %>
  </p>

  <p>
    <%= form.submit "New Podcast" %>
  </p>

<% end %>
