<% commentable.comments.each do |comment| %>
  <p><strong><%= comment.user.profile.first_name %> - </strong><%= comment.body %></p>
  <%= link_to 'Destroy Comment', [comment.user, comment],
               method: :delete,
               data: { confirm: 'Are you sure?' } %>
<% end %>



</p>
